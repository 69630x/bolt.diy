/** @type {import('@remix-run/dev').AppConfig} */
module.exports = {
  // Use the default Node.js server (compatible with Vercel)
  // Remove Cloudflare-specific lines if present
  // serverBuildTarget: "vercel", // optional, Vercel detects automatically
};